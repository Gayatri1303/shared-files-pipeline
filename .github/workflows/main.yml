name: Hello World

on:
  workflow_dispatch:
  push:
    branches:
      - main  

jobs:
  say-hello:
    runs-on: self-hosted
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: print hello
        run: echo "hello" > /home/ubuntu/hello.txt
